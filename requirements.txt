# ============================================================
# 24 HORAS DE ADORAÇÃO — STORE
# Lista de dependências (referência)
#
# ATENÇÃO: este é um projeto Node.js/Next.js, NÃO Python.
# A fonte real de verdade é o package.json + package-lock.json.
# Para instalar tudo, rode:  npm install
# (este requirements.txt é só uma referência legível)
# ============================================================

# ---------- Pré-requisitos (runtime) ----------
node.js == 22.23.1        # instalado portátil em ~/nodejs (sem admin)
npm == 10.9.8

# ---------- Dependências de produção ----------
next==14.2.15                 # framework React (App Router)
react==18.3.1
react-dom==18.3.1
three==0.169.0                # engine 3D
@react-three/fiber==8.17.10   # React renderer para Three.js
@react-three/drei==9.114.3    # helpers R3F (useGLTF, OrbitControls, etc.)
gsap==3.12.5                  # animações ligadas ao scroll
framer-motion==11.11.9        # animações/reveals/gestos
lenis==1.1.17                 # scroll suave
zustand==4.5.5                # estado global (mood, carrinho, scrollProgress)
lucide-react==0.451.0         # ícones

# ---------- Dependências de desenvolvimento ----------
typescript==5.6.3
@types/node==22.7.5
@types/react==18.3.11
@types/react-dom==18.3.0
tailwindcss==3.4.14           # estilos utilitários
postcss==8.4.47
autoprefixer==10.4.20
playwright-core==1.48.0       # opcional: usado só para testes/screenshots headless
